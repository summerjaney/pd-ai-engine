# 表单字段联动规则复制

## 背景

表单设计器已经具备字段联动能力。配置复杂表单时，不同目标字段可能使用相近的联动条件，重复配置效率较低。

## 目标

支持复制已有字段联动规则，修改目标字段后形成新规则，并保留规则来源和校验结果。

## 约束

- 复制操作属于字段联动能力增强。
- 复制后的规则必须重新校验字段是否属于当前数据模型。
- 不复制已经失效或包含循环依赖的规则。
- 发布前需要在运行态预览复制后的联动结果。
