# PAE v1.1.0 发布前检查报告

## 1. 发布门槛

| 检查项 | 要求 | 状态 |
|---|---|---|
| 版本一致性 | package、CLI、manifest 均为 1.1.0 | PASS |
| TypeScript | 构建与无输出类型检查通过 | PASS |
| 自动化测试 | 全量测试 100% 通过 | PASS |
| 连续产品演进 | 同一产品三个连续需求正确累积 | PASS |
| 基线安全 | 哈希、快照、过期检查及 ERROR 门禁通过 | PASS |
| 历史兼容 | v0.3.0—v1.0.0 测试无回退 | PASS |
| npm 发布包 | 包内容、独立安装及 CLI 启动通过 | PASS |
| 前序版本 | v1.0.0 已合并并正式发布 | BLOCKED |

## 2. 阻断项

当前仓库不存在 `v1.0.0` Tag，`main` 尚未包含 `feat/v1.0.0-stable-delivery` 的正式发布提交。v1.1.0 不应越过 v1.0.0 直接创建 Tag 或 Release。

## 3. 正确发布顺序

1. 推送并合并 `feat/v1.0.0-stable-delivery` 到 `main`。
2. 在更新后的 `main` 重跑 v1.0.0 门禁，创建 `v1.0.0` Tag 和 Release。
3. 将 v1.1.0 分支整理到正式 v1.0.0 基线之上并处理可能的差异。
4. 重跑 v1.1.0 全量测试及 npm 独立安装检查。
5. 创建 v1.1.0 PR；合并后创建 `v1.1.0` Tag 和 Release。

## 4. 当前结论

v1.1.0 本地候选功能验收通过，但远端正式发布状态为 `BLOCKED`，唯一发布阻断项是前序 v1.0.0 尚未正式发布。
